general {
    # Generated by Hypr-Lab Settings. Edit power.conf through the UI instead.
    lock_cmd = quickshell -p ~/.config/quickshell/lock/shell.qml
    inhibit_sleep = 3
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'

    ignore_dbus_inhibit = false
    ignore_systemd_inhibit = false
    ignore_wayland_inhibit = false
}

# Automatic Hypr-Lab lock after 5 minute(s) idle.
listener {
    timeout = 300
    on-timeout = loginctl lock-session
}

# Display standby 5 minute(s) after the automatic lock point.
listener {
    timeout = 600
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

# Suspend 20 minute(s) after the automatic lock point.
listener {
    timeout = 1500
    on-timeout = systemctl suspend
}
